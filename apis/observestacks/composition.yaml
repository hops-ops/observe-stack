apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: observestacks.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: hops.ops.com.ai/v1alpha1
    kind: ObserveStack
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-observe-stackrender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
